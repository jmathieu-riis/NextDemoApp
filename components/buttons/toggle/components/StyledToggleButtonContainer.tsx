import styled from 'styled-components';

export const StyledToggleButtonContainer = styled.div(() => ({
  width: '2em',
  height: '1em',
  marginTop: 'auto',
  marginBottom: 'auto',
  cursor: 'pointer',
}));
